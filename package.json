{
    "name": "clothing",
    "scripts": {
        "start": "cd frontend && npm run start",
        "heroku-postbuild": "cd frontend && npm install && npm run start && cd ../backend && npm install"
    }
}